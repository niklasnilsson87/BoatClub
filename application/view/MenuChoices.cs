public enum MainMenu
{
    AddMember = 1, ViewMembers, RemoveMember, ChangeMember, SearchMember, AddBoat, RemoveBoat, ChangeBoat, SaveExit
}

public enum MemberListTypes
{
    Compact = 1, Verbose
}

public enum AssignBoatMenu
{
    Select = 1, Search
}